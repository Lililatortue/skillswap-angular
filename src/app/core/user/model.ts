export type User = {
    id            : string;
    name          : string;
    username      : string;
    bio           : string;
    skills        : string[];
    rating_avg    : number;
    completed_jobs: number;
}
